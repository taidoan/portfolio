import type { HeroBlockProps } from '@/payload-types';

export type MediumHeroProps = HeroBlockProps;

export const MediumHero = ({ image, richText }: MediumHeroProps) => {
  return <div>Medium Hero</div>;
};

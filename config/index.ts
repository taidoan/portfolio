import { root, source, styles, pub, assets, images } from './paths';

/**
 * Configuration object containing paths for the project
 */
export const config = {
  rootPath: root,
  srcPath: source,
  stylesPath: styles,
  publicPath: pub,
  assetsPath: assets,
  imagesPath: images,
};
